// (맨왼쪽->0) + (0->no_t)
    // sum += (d[0]) + (no_t - 1);

    // printf("->%d->0->%d", d[0], no_t-1);

    // for (i = n - 1; i >= previous; i--)
    // {
    //   printf("->");
    //   printf("%d", d[i]);

    //   if(i > previous) sum += abs(d[i] - d[i - 1]);
    // }